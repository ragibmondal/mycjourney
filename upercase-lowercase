#include <stdio.h>
int main(){
  char ch;
  printf("enter a letter\n");
  scanf("%c", &ch);


  if (ch>='a' && ch<='z'){
    printf("lower case");
  }
  else if ("ch>='A' && ch<='Z'"){
      printf("uper case");
  }
  else{
      printf("invalid letter");
  }

  return 0;
}
